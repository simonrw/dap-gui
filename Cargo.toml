[workspace]
resolver = "2"

members = [
    "transport",
    "debugger",
    "server", "pcaplog",
]

[profile.release]
debug = 1

[workspace.dependencies]
anyhow = "1.0.77"
spmc = "0.3.0"
tracing = "0.1.40"
serde = { version = "1.0.193", features = ["derive"] }
